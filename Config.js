var tempUnit = "f"; // Replace with c for celcuis.
var hourConfig = false; // Turn on for 24hr.
var WOEID = "12783042"; // Weather code. Just google WOEID
var textColor = "black"; // Change color of text. Enter a color name, hex, or rgba.
var fontSize = 100;
var refreshRate = 1; //Change refresh rate of weather (hours)
var ampmVisable = false; //Turns am and pm visablity on and off
